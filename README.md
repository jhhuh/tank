# Tank

Starting over. The previous terminal multiplexer prototype is archived
on `archive/v0-multiplexer`.

Tank is being reimagined as "Plan 9 in Unix" — a programmable runtime
for composing heterogeneous computing environments through POSIX-like
semantics, wearing the skin of a terminal multiplexer.

Design is in progress.
